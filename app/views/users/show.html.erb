<h3>&nbsp;</h3>
<%= notice %>

<%= form_with(model: @blog, url: blogs_path, local: true) do |form| %>
  
<div class="panel panel-default">
  <div class="panel-heading">
    <%= @page_title %>
  </div>
  <table class="table">
    <tr>
      <th>名前</th>
      <td><%= @blog.name %></td>
    </tr>
    <tr>
      <th><%= form.label :image %></th>
      <td><%= form.file_field :image, id:"profile_image" %></td>
    </tr>
    <tr>
      <th></th>
      <td><%= form.submit "更新する", class: 'btn btn-default' %></td>
    </tr>
  </table>
</div>





  <p></p>
  <p>本文：</p>
  <%= form.submit "更新する", class: 'btn btn-default' %>
<% end %>
